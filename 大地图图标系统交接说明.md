# 大地图图标系统 —— 交接说明

> 交接对象：后续接手本项目的 AI / 开发者
> 生成时间：2026-08-30
> 范围：engine-free-rpg 仓库内，大地图（Large Map）地点图标的渲染与缩放体系

---

## 一、本次完成了什么

修复了大地图地点图标（marker）的几类显示问题，并新增了图标分类缩放能力。最终效果：

| 需求 | 实现结果 |
|------|----------|
| `town.native.*` / `town.city.*` 图标按原图比例输出 | ✅ 底部对齐 + 纹理原始像素尺寸 |
| noevent / `town.waiguo*` 图标紧凑显示 | ✅ 图标 0.7x，label/notice 不缩 |
| 地图名、感叹号、图标缩放时不漂移 | ✅ 底部锚点对齐 |
| native 图标描边（shade）单独可调 | ✅ 专用材质，默认更细 |
| 主角头像缩放行为 | ✅ 跟随 markerScale |

---

## 二、修改的文件清单

只涉及 4 个文件，其余无关改动不属于本次任务。

| 文件 | 改动性质 |
|------|----------|
| `src/Game.Godot/Map/LargeMapMarker.cs` | 核心逻辑（图标分类、缩放、底部对齐、材质） |
| `src/Game.Godot/Map/LargeMapView.cs` | 缩放调度 + 主角头像缩放（仅 2 处） |
| `scenes/map/map_entity_slot.tscn` | 新增 native 专用描边材质 |
| `assets/shader/sprite_outline.gdshader` | 描边 shader（texels 单位，缩放恒定） |

---

## 三、核心设计决策（重要，勿改错）

### 3.1 缩放模型（沿用原有成熟框架）

原始框架（未改动）：所有 marker 统一 `marker.Scale = markerScale`（=`BaseScale.Y * Zoom`），
图标、文字、感叹号一起等比缩放。这是 Godot 里最稳定的模型，**不要**改成"只缩图标不缩文字"之类。

本次只加了两处差异化，**不破坏整体缩放**：

1. **图标类型缩放** `IconScaleFactor`（仅作用 Avatar/OverflowAvatar，不作用 label/notice）
2. **native 图标尺寸**：Avatar 用纹理原始像素 + 底部对齐

### 3.2 底部对齐（关键！对齐魔改引擎）

魔改引擎 `MapLocationUI.SetImageSize` 的 `RectTransform` 用**底部锚点**：
- 图标底部固定在地图坐标点，向上生长
- 文字在底部固定位置，紧贴图标底

对应 Godot 实现（`ApplyIconLayout`）：
```csharp
target.OffsetLeft  = -halfX;                    // 水平居中
target.OffsetRight =  halfX;
target.OffsetTop   = -(size.Y - _visualSize.Y); // 顶部上移全部
target.OffsetBottom = 0f;                        // 底部固定
_iconBottomLocal   = _visualSize.Y;              // 恒 80
```

**曾踩过的坑**：早期用"对称 offsets（中心扩展）"导致 native 图标底部下移 `(size.Y-80)/2`，
文字与图标底部出现间隙。**必须底部对齐，不能中心对齐。**

### 3.3 Avatar 缩放原点（PivotOffset）

`_Ready` 里设置：
```csharp
_avatar.PivotOffset        = new Vector2(_visualSize.X * 0.5f, _visualSize.Y);  // (40, 80)
_overflowAvatar.PivotOffset = new Vector2(_visualSize.X * 0.5f, _visualSize.Y);
```

**必须用底部 (40,80)，不能用中心 (40,40)**。否则 `IconScaleFactor=0.7` 缩放时
图标底部会偏移（中心缩放导致底部上移 12px），与其他图标底部不对齐。

### 3.4 图标分类（IconScaleFactor）

在 `Refresh()` 中判断：
```csharp
IconScaleFactor = IsExtraCompactImage(imageId) || UsesDefaultNoEventTexture(avatar)
    ? 0.7f   // noevent.png 与 town.waiguo*
    : 1f;    // 其余
```
- `IsExtraCompactImage`：imageId 含 "noevent" 或 "waiguo"
- `UsesDefaultNoEventTexture`：纹理 == 场景默认 noevent.png（兜底纹理，数据层无 ID）

### 3.5 native 专用描边材质

native 图标是原尺寸大图，默认描边 `outline_width_texels=4.5` 视觉偏粗，
单独设 `outline_width_texels=2.0`。在 `ApplyIconLayout` 中：
```csharp
target.Material = useNativeSize
    ? NativeOutlineMaterial ?? _defaultAvatarMaterial
    : _defaultAvatarMaterial;
```

### 3.6 地图名（label）位置

label 保持在 Visual 内（不 Reparent），跟 marker 一起缩放（字号随缩放，是 Godot Label 天然行为）：
```csharp
_nameLabel.Position = new Vector2(-35f, _iconBottomLocal - 10f);
```
`-10` 抵消 Label 内部 vertical padding + outline 的约 9.5 屏像素间隙，让文字视觉紧贴图标底。

---

## 四、可调参数位置

### 4.1 native 图标描边粗细

文件：`scenes/map/map_entity_slot.tscn` → `ShaderMaterial_native` sub_resource
```gdscript
shader_parameter/outline_width_texels = 2.0   # 越小越细（默认 4.5）
```
或在 Godot 编辑器选中 `MapEntitySlot` 根节点 → Inspector → `Native Outline Material` 展开调。

### 4.2 紧凑系数（noevent/waiguo）

文件：`src/Game.Godot/Map/LargeMapMarker.cs` → `Refresh()` 中的 `0.7f`

### 4.3 标准图标描边粗细

文件：`scenes/map/map_entity_slot.tscn` → `ShaderMaterial_i7uqr` 的 `outline_width_texels`（默认 4.5）

---

## 五、已知限制（有意接受，勿"修复"）

1. **label 字号跟随地图缩放**（放大变 50px，缩小 25px）。曾尝试 Reparent + Scale 反向补偿让字号恒定，
   但在 Godot 4.2 上 Label 的 layout 上下文会异常（parent 无 size），不可靠。**接受跟随缩放**，
   这是 Godot Label 的天然行为，最稳定。

2. **shader 描边用 texels 单位**（`TEXTURE_PIXEL_SIZE`），缩放时描边与图标等比、视觉粗细恒定。
   代价是不同尺寸图标用同一个 texels 值，视觉粗细略有差异（所以 native 单独设了 2.0）。

---

## 六、验证方法

1. 进入大地图，检查 town.native.*（英雄雕像等）图标：底部与其他图标对齐、向上生长、文字紧贴、有描边
2. noevent / town.waiguo* 图标：底部对齐、图标更小（0.7x）、文字和感叹号不缩
3. 缩放地图（滚轮）：图标/文字/主角头像等比缩放、不漂移
4. 主角头像：跟随缩放、大小合适
